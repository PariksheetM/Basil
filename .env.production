# Production API URL – points to the PHP backend on Hostinger
# The backend folder sits inside the same subfolder as the React build
VITE_API_BASE_URL=https://qsr.catalystsolutions.eco/fooddash/backend/api
