# System Architecture Overview

## 📐 Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (React)                         │
│                      http://localhost:5173                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────┐  ┌────────────────┐  ┌──────────────────┐  │
│  │   LoginPage    │  │   SignupPage   │  │   HomePage       │  │
│  │  (Updated)     │  │  (Updated)     │  │ (Protected)      │  │
│  └────────────────┘  └────────────────┘  └──────────────────┘  │
│                                                                  │
│  ┌────────────────┐  ┌────────────────┐  ┌──────────────────┐  │
│  │  MealBoxPage   │  │  SnackBoxPage  │  │   BowlPage       │  │
│  │  (Protected)   │  │  (Protected)   │  │  (Protected)     │  │
│  └────────────────┘  └────────────────┘  └──────────────────┘  │
│                                                                  │
│  ┌────────────────┐  ┌────────────────┐  ┌──────────────────┐  │
│  │  BuffetPage    │  │  CheckoutPage  │  │   OrdersPage     │  │
│  │  (Protected)   │  │  (Protected)   │  │  (Protected)     │  │
│  └────────────────┘  └────────────────┘  └──────────────────┘  │
│                                                                  │
├─────────────────────────────────────────────────────────────────┤
│                    SERVICES & CONTEXTS                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │              CartContext (Global State)                     │ │
│  │  - cart, cartCount, loading                                 │ │
│  │  - addToCart(), updateCartItem(), removeFromCart()          │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌──────────────────┐          ┌──────────────────────────────┐│
│  │  AuthService     │          │     OrderService             ││
│  │  - signup()      │          │     - getMenu()              ││
│  │  - login()       │          │     - getCart()              ││
│  │  - logout()      │          │     - addToCart()            ││
│  │  - verifySession │          │     - updateCartItem() ✏️    ││
│  │  - isLoggedIn()  │          │     - removeFromCart()       ││
│  │  - getUserData() │          │     - checkout()             ││
│  └──────────────────┘          │     - getOrders()            ││
│                                 └──────────────────────────────┘│
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
                              ▼
                        HTTP Requests
                   (with session tokens)
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    BACKEND (PHP + Apache)                        │
│              http://localhost/Prototype/backend/api              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              config/database.php                         │   │
│  │  - Loads .env file                                       │   │
│  │  - Database connection (PDO)                             │   │
│  │  - CORS headers                                          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌────────────────────── API ENDPOINTS ──────────────────────┐ │
│  │                                                            │ │
│  │  Authentication:                                          │ │
│  │  • POST   /api/signup.php         - Register             │ │
│  │  • POST   /api/login.php          - Login                │ │
│  │  • GET    /api/verify_session.php - Verify               │ │
│  │  • POST   /api/logout.php         - Logout               │ │
│  │                                                            │ │
│  │  Menu:                                                     │ │
│  │  • GET    /api/menu.php           - Get all items        │ │
│  │                                                            │ │
│  │  Cart:                                                     │ │
│  │  • GET    /api/cart.php           - View cart            │ │
│  │  • POST   /api/cart.php           - Add to cart          │ │
│  │  • PUT    /api/cart_item.php      - Update item ✏️       │ │
│  │  • DELETE /api/cart_item.php      - Remove item          │ │
│  │                                                            │ │
│  │  Orders:                                                   │ │
│  │  • POST   /api/checkout.php       - Place order          │ │
│  │  • GET    /api/orders.php         - Order history        │ │
│  │                                                            │ │
│  └────────────────────────────────────────────────────────────┘│
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
                              ▼
                        SQL Queries
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                   DATABASE (MySQL)                               │
│                   food_ordering_db                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────────────┐  ┌────────────────┐  ┌──────────────────┐  │
│  │     users      │  │ user_sessions  │  │ menu_categories  │  │
│  │  - id          │  │  - id          │  │  - id            │  │
│  │  - full_name   │  │  - user_id (FK)│  │  - name          │  │
│  │  - email       │  │  - token       │  │  - description   │  │
│  │  - password    │  │  - expires_at  │  │  - image_url     │  │
│  │  - phone       │  └────────────────┘  └──────────────────┘  │
│  │  - address     │                                             │
│  └────────────────┘                                             │
│                                                                  │
│  ┌────────────────┐  ┌────────────────┐  ┌──────────────────┐  │
│  │  menu_items    │  │    orders      │  │  order_items     │  │
│  │  - id          │  │  - id          │  │  - id            │  │
│  │  - category_id │  │  - user_id (FK)│  │  - order_id (FK) │  │
│  │  - name        │  │  - order_number│  │  - menu_item_id  │  │
│  │  - description │  │  - total_amount│  │  - quantity ✏️   │  │
│  │  - price       │  │  - subtotal    │  │  - unit_price    │  │
│  │  - image_url   │  │  - tax_amount  │  │  - total_price   │  │
│  │  - is_veg      │  │  - status      │  │  - customizations│  │
│  │  - spice_level │  │  - payment     │  │    (JSON) ✏️     │  │
│  │  - calories    │  │  - address     │  │  - instructions  │  │
│  └────────────────┘  │  - phone       │  │    ✏️            │  │
│                      └────────────────┘  └──────────────────┘  │
│                                                                  │
│  Sample Data Included:                                          │
│  • 4 categories (Meal Box, Snack Box, Bowls, Buffet)           │
│  • 15+ menu items with prices and details                       │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow Examples

### 1. User Signup Flow
```
User fills form → SignupPage
                    ↓
              AuthService.signup()
                    ↓
          POST /api/signup.php
                    ↓
        Database: INSERT INTO users
                    ↓
          Response: success/error
                    ↓
         Redirect to login page
```

### 2. User Login Flow
```
User enters credentials → LoginPage
                             ↓
                    AuthService.login()
                             ↓
                 POST /api/login.php
                             ↓
        Database: SELECT user, CREATE session
                             ↓
         Response: session_token + user data
                             ↓
        Store token in localStorage
                             ↓
            Redirect to HomePage
```

### 3. Add to Cart Flow
```
User clicks "Add to Cart" → Any Page
                               ↓
                    useCart().addToCart()
                               ↓
                  OrderService.addToCart()
                               ↓
         POST /api/cart.php (with session token)
                               ↓
  Database: CREATE order (if needed), INSERT order_item
                               ↓
              Response: success + order_id
                               ↓
           CartContext refreshes cart
                               ↓
          Cart count updates globally
```

### 4. Edit Cart Item Flow (⭐ EDITABLE!)
```
User changes quantity/customization → Cart Page
                                         ↓
                        useCart().updateCartItem()
                                         ↓
                     OrderService.updateCartItem()
                                         ↓
        PUT /api/cart_item.php?id=X (with session token)
                                         ↓
           Database: UPDATE order_items SET ...
                                         ↓
              Database: UPDATE orders totals
                                         ↓
                  Response: success
                                         ↓
               CartContext refreshes cart
                                         ↓
                UI updates automatically
```

### 5. Checkout Flow
```
User enters delivery details → CheckoutPage
                                    ↓
                       OrderService.checkout()
                                    ↓
         POST /api/checkout.php (with session token)
                                    ↓
      Database: UPDATE orders SET status='pending'
                                    ↓
                Response: success + order_id
                                    ↓
             CartContext clears cart
                                    ↓
          Redirect to order confirmation
```

## 🔐 Security Flow

```
Every Protected API Request:
    ↓
1. Check Authorization header
    ↓
2. Extract session_token
    ↓
3. Query user_sessions table
    ↓
4. Verify token exists
    ↓
5. Check expires_at > NOW()
    ↓
6. If valid: Get user_id, process request
    ↓
7. If invalid: Return 401 Unauthorized
```

## 🎯 Component Communication

```
                    ┌──────────────┐
                    │     App      │
                    │ (CartProvider│
                    │   wrapper)   │
                    └──────┬───────┘
                           │
         ┌─────────────────┼─────────────────┐
         │                 │                 │
    ┌────▼────┐      ┌────▼────┐      ┌────▼────┐
    │ Login   │      │  Home   │      │ Orders  │
    │ Page    │      │  Page   │      │  Page   │
    └─────────┘      └────┬────┘      └────┬────┘
                          │                 │
                     ┌────▼─────────────────▼────┐
                     │     CartContext          │
                     │  (Shared State)           │
                     │  - cart                   │
                     │  - cartCount              │
                     │  - addToCart()            │
                     │  - updateCartItem()       │
                     │  - removeFromCart()       │
                     └───────────────────────────┘
```

## 📊 Database Relationships

```
users (1) ──────────── (*) user_sessions
  │                           
  │ (1)                       
  │                           
  └──────────── (*) orders
                    │
                    │ (1)
                    │
                    └──────── (*) order_items
                                    │
                                    │ (*)
                                    │
                                    └── (1) menu_items
                                              │
                                              │ (*)
                                              │
                                              └── (1) menu_categories

Legend:
(1) = One
(*) = Many
FK = Foreign Key
```

## ✨ Key Features Visualization

```
┌─────────────────────────────────────────────────────────┐
│                   USER JOURNEY                          │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  1. Sign Up / Login                                     │
│     └─→ Secure authentication ✓                        │
│         Session token stored ✓                          │
│                                                         │
│  2. Browse Menu                                         │
│     └─→ 4 categories ✓                                 │
│         15+ items with details ✓                        │
│                                                         │
│  3. Add to Cart                                         │
│     └─→ Items saved to database ✓                      │
│         Cart count visible ✓                            │
│                                                         │
│  4. Edit Cart ⭐ EDITABLE!                              │
│     └─→ Change quantity ✓                              │
│         Update customizations ✓                         │
│         Modify instructions ✓                           │
│         Remove items ✓                                  │
│                                                         │
│  5. Checkout                                            │
│     └─→ Enter delivery details ✓                       │
│         Choose payment method ✓                         │
│         Place order ✓                                   │
│                                                         │
│  6. Track Orders                                        │
│     └─→ View order history ✓                           │
│         See order status ✓                              │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## 🎉 What Makes This System Special

```
✅ Fully editable cart items (quantity, customizations, instructions)
✅ Real-time cart synchronization across all pages
✅ Secure session-based authentication
✅ Protected routes (no unauthorized access)
✅ Complete order history tracking
✅ Sample data included (ready to test)
✅ Environment configuration (.env)
✅ CORS configured for local development
✅ Comprehensive documentation
✅ Production-ready backend
```
